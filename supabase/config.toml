[functions]
[functions.send-email]
verify_jwt = false

[functions.pawapay-webhook]
verify_jwt = false

[functions.verify-pawapay-payment]
verify_jwt = false

# External webhooks and DB-trigger targets (no Supabase user JWT)
[functions.stripe-webhook]
verify_jwt = false

[functions.paydunya-ipn]
verify_jwt = false

[functions.payment-backup]
verify_jwt = false

[functions.welcome-user]
verify_jwt = false

[functions.validate-ticket]
verify_jwt = false

[functions.image-cache-warmer]
verify_jwt = false

# Upstash Redis-backed event cache — public read, anon key auth
[functions.get-events]
verify_jwt = false

# Upstash Redis-backed categories cache — public read, anon key auth
[functions.get-categories]
verify_jwt = false

# Cache invalidation — auth via service-role key or CACHE_INVALIDATION_SECRET
[functions.invalidate-events-cache]
verify_jwt = false

# ── Payment reliability (Upstash Redis + QStash) ─────────────────────────────

# Redis-first payment status polling (requires valid Supabase JWT from mobile app)
[functions.payment-status]
verify_jwt = true

# Async ticket finalization — called by QStash, auth via X-Finalize-Secret header
[functions.finalize-order]
verify_jwt = false

# PawaPay safety-net poller — called by QStash, auth via X-Finalize-Secret header
[functions.check-pawapay-status]
verify_jwt = false

# Guest ticket retrieval by phone + OTP (no user JWT; OTP is the auth)
[functions.lookup-guest-tickets]
verify_jwt = false

# Attach guest orders to the signed-in account (requires user JWT)
[functions.claim-guest-orders]
verify_jwt = true

# Payment verify/self-heal (Stripe + others). Guests poll this after pay; no user JWT.
[functions.verify-payment]
verify_jwt = false

# Guest card checkout (anon / publishable key). JWT would 401 phone-only guests.
[functions.create-stripe-payment]
verify_jwt = false